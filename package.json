{
  "name": "jooooonkim-portfolio",
  "private": true,
  "description": "Static portfolio site (no build step - this file exists only to wire up local checks).",
  "scripts": {
    "test": "node scripts/check-contrast.js"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "sharp": "^0.35.4"
  },
  "allowScripts": {
    "ffmpeg-static@5.3.0": true
  }
}
